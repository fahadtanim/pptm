import { IProjectProduction } from '../Interfaces/IProjectProduction';

export class ProjectProduction implements IProjectProduction {
  production_id: any;
  production_date: Date;
  production_details: string;
}
